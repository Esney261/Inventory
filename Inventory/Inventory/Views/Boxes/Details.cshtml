@model Inventory.Data.Entities.Box

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
 <partial name="_BoxDetails"/>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id"class="btn btn-outline-primary">Agregar Departamento</a> 
    <a asp-action="AddState" asp-route-id="@Model?.Id"class="btn btn-outline-warning">Editar Pais</a> 
    <a asp-action="Index"class="btn btn-outline-success">Volver atras</a>
</div>
